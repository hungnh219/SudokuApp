import 'package:flutter/widgets.dart';

class ModalCustom extends StatelessWidget {
  const ModalCustom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}